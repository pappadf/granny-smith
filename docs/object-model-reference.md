# Object Model Reference

> Auto-generated by `scripts/dump_object_model.py` from the live
> headless emulator. Do not edit by hand — re-run the script after
> any change to a class table or root method.

Each section names a node in the tree by its dotted path. Members
are grouped by kind: attributes (read-only or read-write), methods
(with their arg list and return type), and child objects.

## `(root)` *(class emu)*

**Methods**
- **`objects(`path?: string`)` → `list`** — List child object names at the given path (or root)
- **`attributes(`path?: string`)` → `list`** — List attribute names of the resolved object's class
- **`methods(`path?: string`)` → `list`** — List method names of the resolved object's class
- **`help(`path?: string`)` → `string`** — Return the doc string of a resolved member (or class name)
- **`time()` → `uint`** — Wall-clock seconds since the Unix epoch
- **`print(`value: none`)` → `string`** — Format a value as a string for display
- **`cp(`src: string`, `dst: string`, `flags?: string`)` → `bool`** — Copy a file or directory (alias for storage.import / legacy `cp`)
- **`peeler(`path: string`, `out_dir?: string`)` → `bool`** — Extract a Mac archive (.sit/.cpt/.hqx/.bin)
- **`peeler_probe(`path: string`)` → `bool`** — True if a file is a peeler-supported archive
- **`hd_create(`path: string`, `size: string`)` → `bool`** — Create a blank SCSI hard-disk image
- **`hd_download(`path: string`)` → `none`** — Download an HD image (deferred — needs platform plumbing)
- **`rom_probe(`path?: string`)` → `bool`** — True if a file is a recognised ROM image (no arg = is a ROM loaded?)
- **`rom_validate(`path: string`)` → `bool`** — Verify a ROM file's checksum and recognised model
- **`vrom_probe(`path: string`)` → `bool`** — True if a file is a recognised video-ROM image
- **`vrom_validate(`path: string`)` → `bool`** — Verify a video-ROM file's signature
- **`fd_probe(`path: string`)` → `bool`** — True if a file is a recognised floppy-disk image
- **`find_media(`dir: string`, `dst?: string`)` → `bool`** — Search a directory for a floppy image; copy to dst if given
- **`partmap(`path: string`)` → `bool`** — Parse and print the Apple Partition Map of a disk image
- **`probe(`path: string`)` → `bool`** — Probe an image for its format (HFS / ISO / APM / ...)
- **`list_partitions(`path: string`)` → `bool`** — List partitions cached for a mounted image
- **`unmount(`path: string`)` → `bool`** — Force-close a cached auto-mount of an image
- **`quit()` → `none`** — Exit the emulator (asks the legacy quit command to end the run)
- **`checkpoint_probe()` → `bool`** — True if a valid checkpoint exists for the active machine
- **`checkpoint_clear()` → `bool`** — Remove all checkpoint files for the active machine
- **`checkpoint_load(`path?: string`)` → `bool`** — Load a checkpoint (auto-loads latest when path is omitted)
- **`checkpoint_save(`path: string`, `mode?: string`)` → `bool`** — Save the current machine state to a checkpoint file
- **`register_machine(`id: string`, `created: string`)` → `bool`** — Register the active machine identity (must precede any image open)
- **`running()` → `bool`** — True if the scheduler is currently running
- **`rom_checksum(`path: string`)` → `string`** — Return the 8-char hex checksum of a ROM file (empty on invalid)
- **`rom_load(`path: string`)` → `bool`** — Load a ROM file and create the matching machine
- **`fd_insert(`path: string`, `slot: int`, `writable?: bool`)` → `bool`** — Insert a floppy image into a drive slot
- **`run(`cycles?: uint`)` → `bool`** — Start the scheduler (optionally for a cycle budget)
- **`vrom_load(`path: string`)` → `bool`** — Set the video-ROM path for the next machine init
- **`hd_attach(`path: string`, `id: int`)` → `bool`** — Attach a hard-disk image at the given SCSI id
- **`hd_validate(`path: string`)` → `bool`** — True if the file is a recognised hard-disk image
- **`cdrom_validate(`path: string`)` → `bool`** — True if the file is a recognised CD-ROM image
- **`fd_validate(`path: string`)` → `string`** — Floppy density tag ("400K" / "800K" / "1.4MB") or empty if unrecognised
- **`setup_machine(`model: string`, `ram_kb?: uint`)` → `bool`** — Prime the next `rom_load` for a specific machine model
- **`schedule(`mode: string`)` → `bool`** — Set the scheduler mode (max / realtime / hardware)
- **`download(`path: string`)` → `bool`** — Trigger a browser file download (WASM-only)
- **`cdrom_attach(`path: string`)` → `bool`** — Attach a CD-ROM image to the SCSI bus
- **`hd_models()` → `string`** — Return the known SCSI HD model catalog as a JSON array string
- **`dump_tree()` → `string`** — Walk the live object tree and return one JSON description of every node

### `input` *(class input)*

#### `input.mouse` *(class mouse)*

**Methods**
- **`move(`x: int`, `y: int`)` → `none`** — Set mouse position (per-platform default route)
- **`click(`down?: bool`)` → `none`** — Press or release the mouse button via the hardware path
- **`trace(`enabled: bool`)` → `none`** — Toggle the 1 Hz mouse-position trace logger

### `network` *(class network)*

#### `network.appletalk` *(class appletalk)*

##### `network.appletalk.printer` *(class atalk_printer)*

**Attributes**
- **enabled** *(attr, bool, ro)*
- **name** *(attr, string, ro)*

**Methods**
- **`enable(`name?: string`)` → `none`** — Advertise the LaserWriter via NBP
- **`disable()` → `none`** — Stop advertising the LaserWriter

##### `network.appletalk.shares` *(class atalk_shares)*

**Methods**
- **`add(`name: string`, `path: string`)` → `none`** — Add an AppleShare volume
- **`remove(`name: string`)` → `none`** — Remove an AppleShare volume by name

**Class children**
- **entries** *(child (indexed) → atalk_share)*

### `sound` *(class sound)*

**Attributes**
- **enabled** *(attr, bool, rw)* — Sound output gate (writable mirror of mute)
- **volume** *(attr, uint, rw)* — Output level (0..7)
- **sample_rate** *(attr, uint, ro)* — Output sample rate in Hz

**Methods**
- **`mute(`muted: bool`)` → `none`** — Mute or unmute the sound output

### `floppy` *(class floppy)*

**Attributes**
- **type** *(attr, enum, ro)* — Controller type: iwm (Plus) or swim (SE/30)
- **sel** *(attr, bool, ro)* — VIA-driven head-select signal

#### `floppy.drives` *(class floppy_drives)*

**Class children**
- **entries** *(child (indexed) → floppy_drive)*

### `scsi` *(class scsi)*

**Attributes**
- **loopback** *(attr, bool, rw)* — Loopback test card / passive terminator

#### `scsi.devices` *(class scsi_devices)*

**Class children**
- **entries** *(child (indexed) → scsi_device)*

#### `scsi.bus` *(class scsi_bus)*

**Attributes**
- **phase** *(attr, enum, ro)*
- **target** *(attr, int, ro)*
- **initiator** *(attr, int, ro)*

### `via1` *(class via)*

**Attributes**
- **ifr** *(attr, uint, ro)*
- **ier** *(attr, uint, ro)*
- **acr** *(attr, uint, ro)*
- **pcr** *(attr, uint, ro)*
- **sr** *(attr, uint, ro)*
- **freq_factor** *(attr, uint, ro)*

#### `via1.port_b` *(class via_port)*

**Attributes**
- **output** *(attr, uint, ro)*
- **input** *(attr, uint, ro)*
- **direction** *(attr, uint, ro)*

#### `via1.port_a` *(class via_port)*

**Attributes**
- **output** *(attr, uint, ro)*
- **input** *(attr, uint, ro)*
- **direction** *(attr, uint, ro)*

### `rtc` *(class rtc)*

**Attributes**
- **time** *(attr, uint, rw)* — Mac-epoch seconds (1904-based); writable
- **read_only** *(attr, bool, ro)* — Write-protect bit
- **pram** *(attr, bytes, ro)* — 256-byte PRAM snapshot

**Methods**
- **`pram_read(`addr?: uint`)` → `uint`** — Read one PRAM byte
- **`pram_write(`addr?: uint`, `value?: uint`)` → `none`** — Write one PRAM byte (honors the write-protect bit)

### `scc` *(class scc)*

**Attributes**
- **loopback** *(attr, bool, rw)* — External loopback (port A TX → port B RX, port B TX → port A RX)
- **pclk_hz** *(attr, uint, ro)* — PCLK source frequency (Hz)
- **rtxc_hz** *(attr, uint, ro)* — RTxC source frequency (Hz)

**Methods**
- **`reset()` → `none`** — Reset the SCC (both channels)

#### `scc.b` *(class scc_channel)*

**Attributes**
- **index** *(attr, int, ro)*
- **dcd** *(attr, bool, ro)*
- **tx_empty** *(attr, bool, ro)*
- **rx_pending** *(attr, uint, ro)*

#### `scc.a` *(class scc_channel)*

**Attributes**
- **index** *(attr, int, ro)*
- **dcd** *(attr, bool, ro)*
- **tx_empty** *(attr, bool, ro)*
- **rx_pending** *(attr, uint, ro)*

### `debugger` *(class debugger)*

#### `debugger.watches` *(class watches)*

#### `debugger.logpoints` *(class logpoints)*

**Methods**
- **`clear()` → `none`** — Remove every logpoint

**Class children**
- **entries** *(child (indexed) → logpoint)*

#### `debugger.breakpoints` *(class breakpoints)*

**Methods**
- **`add(`addr?: uint`, `condition?: string`)` → `object`** — Add a logical-space breakpoint
- **`clear()` → `none`** — Remove every breakpoint

**Class children**
- **entries** *(child (indexed) → breakpoint)*

### `mac` *(class mac)*

**Attributes**
- **BusErrVct** *(attr, uint, ro)* — Bus error vector address
- **MonkeyLives** *(attr, uint, ro)* — System self-test status; >= 0 is OK
- **SysCom** *(attr, uint, ro)* — System communication area (region marker)
- **ScrVRes** *(attr, uint, ro)* — Screen vertical dots per inch
- **ScrHRes** *(attr, uint, ro)* — Screen horizontal dots per inch
- **ScreenRow** *(attr, uint, ro)* — Row bytes of main screen
- **MemTop** *(attr, uint, ro)* — Top of usable RAM
- **BufPtr** *(attr, uint, ro)* — Top of application memory
- **StkLowPt** *(attr, uint, ro)* — Lowest stack pointer value seen
- **HeapEnd** *(attr, uint, ro)* — Pointer to the end of the heap
- **TheZone** *(attr, uint, ro)* — Pointer to the current heap zone
- **UTableBase** *(attr, uint, ro)* — Pointer to the unit I/O table
- **MacJmp** *(attr, uint, ro)* — MacsBug jump table
- **DskRtnAdr** *(attr, uint, ro)* — Used by disk driver
- **PollRtnAddr** *(attr, uint, ro)* — ‘Other’ driver locals
- **DskVerify** *(attr, uint, ro)* — Flag for disk read/verify
- **LoadTrap** *(attr, uint, ro)* — Flag to trap before launch
- **MmInOK** *(attr, uint, ro)* — Memory Manager initial checks OK
- **CPUFlag** *(attr, uint, ro)* — CPU type identifier
- **ApplLimit** *(attr, uint, ro)* — Application heap limit address
- **SonyVars** *(attr, uint, ro)* — Variables for .SONY driver
- **PWMValue** *(attr, uint, ro)* — Current PWM value
- **PollStack** *(attr, uint, ro)* — SCC poll data start stack location
- **PollProc** *(attr, uint, ro)* — SCC poll data procedure
- **DskErr** *(attr, uint, ro)* — Last disk routine error code
- **SysEvtMask** *(attr, uint, ro)* — System event mask
- **SysEvtBuf** *(attr, uint, ro)* — Pointer to system event buffer
- **EventQueue** *(attr, bytes, ro)* — Event queue header
- **EvtBufCnt** *(attr, uint, ro)* — Max events in system event buffer
- **RndSeed** *(attr, uint, ro)* — Random number generator seed
- **SysVersion** *(attr, uint, ro)* — Version number of RAM-based system
- **SEvtEnb** *(attr, uint, ro)* — SystemEvent call enable/disable flag
- **DSWndUpdate** *(attr, uint, ro)* — Flag to prevent drawing behind alert
- **FontFlag** *(attr, uint, ro)* — Font manager loop flag
- **IntFlag** *(attr, uint, ro)* — Flag to reduce interrupt disable time
- **VBLQueue** *(attr, bytes, ro)* — VBL queue header
- **Ticks** *(attr, uint, ro)* — System ticks since startup
- **MBTicks** *(attr, uint, ro)* — Tick count at last mouse down event
- **MBState** *(attr, uint, ro)* — Current state of the mouse button
- **Tocks** *(attr, uint, ro)* — Lisa sub-tick count
- **KeyMap** *(attr, bytes, ro)* — Bitmap of the keyboard state
- **KeypadMap** *(attr, uint, ro)* — Bitmap of the numeric keypad state
- **KeyLast** *(attr, uint, ro)* — ASCII code of last valid keypress
- **KeyTime** *(attr, uint, ro)* — Tick count at last keypress
- **KeyRepTime** *(attr, uint, ro)* — Tick count of last key repeat
- **KeyThresh** *(attr, uint, ro)* — Key repeat threshold
- **KeyRepThresh** *(attr, uint, ro)* — Key repeat rate
- **Lvl1DT** *(attr, bytes, ro)* — Level-1 interrupt dispatch table
- **Lvl2DT** *(attr, bytes, ro)* — Level-2 interrupt dispatch table
- **UnitNtryCnt** *(attr, uint, ro)* — Number of entries in unit table
- **VIA** *(attr, uint, ro)* — VIA base address
- **SCCRd** *(attr, uint, ro)* — SCC base read address
- **SCCWr** *(attr, uint, ro)* — SCC base write address
- **IWM** *(attr, uint, ro)* — IWM (floppy controller) base address
- **GetParam** *(attr, bytes, ro)* — System parameter scratch area
- **Scratch20** *(attr, bytes, ro)* — 20-byte scratchpad area
- **SPValid** *(attr, uint, ro)* — Parameter RAM validity status
- **SysParam** *(attr, bytes, ro)* — Low-memory copy of PRAM
- **SPATalkA** *(attr, uint, ro)* — AppleTalk node hint for modem port
- **SPATalkB** *(attr, uint, ro)* — AppleTalk node hint for printer port
- **SPConfig** *(attr, uint, ro)* — Serial port A & B config bits
- **SPPortA** *(attr, uint, ro)* — Modem port (SCC A) configuration
- **SPPortB** *(attr, uint, ro)* — Printer port (SCC B) configuration
- **SPAlarm** *(attr, uint, ro)* — Alarm clock time setting
- **SPFont** *(attr, uint, ro)* — Default application font number - 1
- **SPKbd** *(attr, uint, ro)* — Keyboard repeat threshold and rate
- **SPPrint** *(attr, uint, ro)* — Printer connection settings
- **SPVolCtl** *(attr, uint, ro)* — PRAM speaker volume setting
- **SPClikCaret** *(attr, uint, ro)* — PRAM double-click and caret times
- **SPMisc1** *(attr, uint, ro)* — Miscellaneous PRAM byte 1
- **PCDeskPat** *(attr, uint, ro)* — Desktop pattern (top bit only)
- **SPMisc2** *(attr, uint, ro)* — Miscellaneous PRAM byte 2
- **Time** *(attr, uint, ro)* — Seconds since midnight, Jan 1, 1904
- **BootDrive** *(attr, uint, ro)* — Drive number of the boot drive
- **JShell** *(attr, uint, ro)* — Journaling shell state
- **SFSaveDisk** *(attr, uint, ro)* — Last vRefNum used by Standard File
- **HiKeyLast** *(attr, uint, ro)* — Keyboard variables alias
- **KbdVars** *(attr, uint, ro)* — Keyboard manager internal variables
- **KbdLast** *(attr, uint, ro)* — Keyboard variables alias (offset +2)
- **JKybdTask** *(attr, uint, ro)* — Pointer to keyboard VBL task
- **KbdType** *(attr, uint, ro)* — Keyboard model number
- **AlarmState** *(attr, uint, ro)* — Alarm clock status flags
- **MemErr** *(attr, uint, ro)* — Last Memory Manager error code
- **JFigTrkSpd** *(attr, uint, ro)* — Jump entry for FMFontMetrics
- **JDiskPrime** *(attr, uint, ro)* — Disk driver vector
- **JRdAddr** *(attr, uint, ro)* — Disk driver vector
- **JRdData** *(attr, uint, ro)* — Disk driver vector
- **JWrData** *(attr, uint, ro)* — Disk driver vector
- **JSeek** *(attr, uint, ro)* — Disk driver vector
- **JSetUpPoll** *(attr, uint, ro)* — Disk driver vector
- **JRecal** *(attr, uint, ro)* — Disk driver vector
- **JControl** *(attr, uint, ro)* — Disk driver vector
- **JWakeUp** *(attr, uint, ro)* — Disk driver vector
- **JReSeek** *(attr, uint, ro)* — Disk driver vector
- **JMakeSpdTbl** *(attr, uint, ro)* — Disk driver vector
- **JAdrDisk** *(attr, uint, ro)* — Disk driver vector
- **JSetSpeed** *(attr, uint, ro)* — Disk driver vector
- **NiblTbl** *(attr, uint, ro)* — End of disk routine vectors
- **FlEvtMask** *(attr, uint, ro)* — Mask of allowable events to flush at FlushEvents
- **SdVolume** *(attr, uint, ro)* — Current speaker volume (0-7)
- **SdEnable** *(attr, uint, ro)* — Sound enabled? (byte)
- **SoundPtr** *(attr, uint, ro)* — Pointer to four-tone sound record
- **SoundVars** *(attr, bytes, ro)* — Sound driver variables (region)
- **SoundBase** *(attr, uint, ro)* — Pointer to free-form synth buffer
- **SoundVBL** *(attr, bytes, ro)* — Sound VBL task control element
- **SoundDCE** *(attr, uint, ro)* — Pointer to sound driver DCE
- **SoundActive** *(attr, uint, ro)* — Flag indicating sound is active
- **SoundLevel** *(attr, uint, ro)* — Current amplitude in sound buffer
- **CurPitch** *(attr, uint, ro)* — Current pitch for square-wave synth
- **Switcher** *(attr, bytes, ro)* — Used by switcher
- **SwitcherTPtr** *(attr, uint, ro)* — Pointer to Switcher's table
- **RSDHndl** *(attr, uint, ro)* — Resource driver handle (-1 until initialized)
- **ROM85** *(attr, uint, ro)* — Flag for ROM version >= $75
- **PortAUse** *(attr, uint, ro)* — Usage status for serial port A
- **PortBUse** *(attr, uint, ro)* — Usage status for serial port B
- **ScreenVars** *(attr, bytes, ro)* — Screen driver variables (MacsBug)
- **JGNEFilter** *(attr, uint, ro)* — Pointer to GetNextEvent filter proc
- **Key1Trans** *(attr, uint, ro)* — Pointer to keyboard translator proc
- **Key2Trans** *(attr, uint, ro)* — Pointer to keypad translator proc
- **SysZone** *(attr, uint, ro)* — Pointer to the system heap zone
- **ApplZone** *(attr, uint, ro)* — Pointer to the application heap zone
- **ROMBase** *(attr, uint, ro)* — Base address of the ROM
- **RAMBase** *(attr, uint, ro)* — Base address for RAM-based traps
- **ExpandMem** *(attr, uint, ro)* — Pointer to expanded memory block
- **DSAlertTab** *(attr, uint, ro)* — Pointer to system error alert table
- **ExtStsDT** *(attr, bytes, ro)* — SCC external/status dispatch table
- **SCCASts** *(attr, uint, ro)* — Last SCC A external/status interrupt
- **SCCBSts** *(attr, uint, ro)* — Last SCC B external/status interrupt
- **SerialVars** *(attr, bytes, ro)* — Async serial driver variables
- **ABusVars** *(attr, uint, ro)* — Pointer to AppleTalk variables
- **ABusDCE** *(attr, uint, ro)* — Pointer to AppleTalk DCE
- **FinderName** *(attr, bytes, ro)* — The name of the Finder [String[15]]
- **DoubleTime** *(attr, uint, ro)* — Double-click interval in ticks
- **CaretTime** *(attr, uint, ro)* — Caret blink interval in ticks
- **ScrDmpEnb** *(attr, uint, ro)* — Screen dump enabled flag
- **ScrDmpType** *(attr, uint, ro)* — Screen dump type (full/window)
- **TagData** *(attr, bytes, ro)* — Sector tag info for disk drivers
- **BufTgFNum** *(attr, uint, ro)* — File tags buffer: file number
- **BufTgFFlg** *(attr, uint, ro)* — File tags buffer: flags
- **BufTgFBkNum** *(attr, uint, ro)* — File tags buffer: logical block num
- **BufTgDate** *(attr, uint, ro)* — File tags buffer: modification date
- **DrvQHdr** *(attr, bytes, ro)* — Queue header for installed drives
- **PWMBuf2** *(attr, uint, ro)* — Pointer to PWM sound buffer
- **HpChk** *(attr, uint, ro)* — Pointer to heap check routine
- **Lo3Bytes** *(attr, uint, ro)* — Constant value $00FFFFFF
- **MaskBC** *(attr, uint, ro)* — Memory Manager byte count mask
- **MaskHandle** *(attr, uint, ro)* — Memory Manager handle mask
- **MaskPtr** *(attr, uint, ro)* — Memory Manager pointer mask
- **MinStack** *(attr, uint, ro)* — Minimum stack size for application
- **DefltStack** *(attr, uint, ro)* — Default stack size for application
- **MMDefFlags** *(attr, uint, ro)* — Default zone flags
- **GZRootHnd** *(attr, uint, ro)* — Root handle for GrowZone
- **GZRootPtr** *(attr, uint, ro)* — Root pointer for GrowZone
- **GZMoveHnd** *(attr, uint, ro)* — Moving handle for GrowZone
- **DSDrawProc** *(attr, uint, ro)* — Pointer to alternate syserror draw proc
- **EjectNotify** *(attr, uint, ro)* — Pointer to disk eject notification proc
- **IAZNotify** *(attr, uint, ro)* — Pointer to world swap notification proc
- **FileVars** *(attr, bytes, ro)* — File system variables
- **NxtDB** *(attr, uint, ro)* — Word used when searching a directory
- **MaxDB** *(attr, uint, ro)* — File Manager private
- **FlushOnly** *(attr, uint, ro)* — Flag used by UnMountVol, FlushVol
- **RegRsrc** *(attr, uint, ro)* — Flag used by File Manager
- **FLckUnlck** *(attr, uint, ro)* — Flag used by SetFilLock, RstFilLock
- **FrcSync** *(attr, uint, ro)* — When set, all File System calls are synched
- **NewMount** *(attr, uint, ro)* — Used by MountVol to flag new mounts
- **DrMstrBlk** *(attr, uint, ro)* — Master directory block in a volume (MFS)
- **FCBSPtr** *(attr, uint, ro)* — Length word of the file-control-block buffer
- **DefVCBPtr** *(attr, uint, ro)* — Default volume’s volume control block
- **VCBQHdr** *(attr, uint, ro)* — Header of the volume-control-block queue
- **FSQHdr** *(attr, uint, ro)* — Header of the file I/O queue
- **FSQHead** *(attr, uint, ro)* — First queued command in File System queue
- **FSQTail** *(attr, uint, ro)* — Last File System queue element
- **RgSvArea** *(attr, bytes, ro)* — Register save area used by system
- **HFSFlags** *(attr, uint, ro)* — Byte of internal HFS flags
- **CurDirStore** *(attr, uint, ro)* — Saved directory for Standard File
- **ErCode** *(attr, uint, ro)* — Disk driver async errors
- **Params** *(attr, bytes, ro)* — Used by the device manager for I/O param blocks
- **FSTemp8** *(attr, bytes, ro)* — Used by File System
- **FSTemp4** *(attr, uint, ro)* — Used by File System
- **FSQueueHook** *(attr, uint, ro)* — Hook to capture all File System calls
- **ExtFSHook** *(attr, uint, ro)* — Used by external file system
- **DskSwtchHook** *(attr, uint, ro)* — Hook for disk switch dialog
- **ReqstVol** *(attr, uint, ro)* — VCB of off-line or external volume
- **ToExtFS** *(attr, uint, ro)* — Memory of an external File System
- **FSVarEnd** *(attr, uint, ro)* — End of File System variables
- **DSAlertRect** *(attr, bytes, ro)* — Rectangle for system error alerts
- **JHideCursor** *(attr, uint, ro)* — Vector used by QuickDraw
- **JShowCursor** *(attr, uint, ro)* — Vector used by QuickDraw
- **JShieldCursor** *(attr, uint, ro)* — Vector used by QuickDraw
- **JScrnAddr** *(attr, uint, ro)* — Vector used by QuickDraw
- **JScrnSize** *(attr, uint, ro)* — Vector used by QuickDraw
- **JInitCrsr** *(attr, uint, ro)* — Vector used by QuickDraw
- **JSetCrsr** *(attr, uint, ro)* — Vector to routine that sets normal cursor
- **JCrsrObscure** *(attr, uint, ro)* — Vector used by QuickDraw
- **JUpdateProc** *(attr, uint, ro)* — Vector used by QuickDraw
- **ScrnBase** *(attr, uint, ro)* — Base address of main screen buffer
- **MTemp** *(attr, uint, ro)* — Raw mouse location from interrupts
- **RawMouse** *(attr, uint, ro)* — Un-jerked mouse coordinates
- **Mouse** *(attr, uint, ro)* — Processed mouse coordinates
- **CrsrPin** *(attr, bytes, ro)* — Cursor pinning rectangle
- **CrsrRect** *(attr, bytes, ro)* — Cursor hit rectangle
- **TheCrsr** *(attr, bytes, ro)* — Current cursor data
- **CrsrAddr** *(attr, uint, ro)* — Saved data from under the cursor
- **NewCrsrJTbl** *(attr, bytes, ro)* — Jump table for new cursor routines
- **JAllocCrsr** *(attr, uint, ro)* — Vector to cursor allocation routine
- **CrsrSave** *(attr, bytes, ro)* — Saved screen data under cursor
- **JSetCCrsr** *(attr, uint, ro)* — Vector to set color cursor routine
- **JOpcodeProc** *(attr, uint, ro)* — Vector for new picture opcodes
- **CrsrBase** *(attr, uint, ro)* — Screen base address for cursor
- **CrsrDevice** *(attr, uint, ro)* — GDevice for the current cursor
- **SrcDevice** *(attr, uint, ro)* — Source GDevice for StretchBits
- **MainDevice** *(attr, uint, ro)* — The main screen's GDevice
- **DeviceList** *(attr, uint, ro)* — Linked list of GDevices
- **CrsrRow** *(attr, uint, ro)* — Row bytes for cursor's screen
- **QDColors** *(attr, uint, ro)* — Handle to default QD colors
- **CrsrVis** *(attr, uint, ro)* — Cursor visibility flag
- **CrsrBusy** *(attr, uint, ro)* — Cursor lockout semaphore
- **CrsrNew** *(attr, uint, ro)* — Flag indicating cursor has changed
- **CrsrCouple** *(attr, uint, ro)* — Flag for cursor coupled to mouse
- **CrsrState** *(attr, uint, ro)* — Cursor show/hide nesting level
- **CrsrObscure** *(attr, uint, ro)* — Cursor obscure semaphore
- **CrsrScale** *(attr, uint, ro)* — Flag for cursor scaling enabled
- **MouseMask** *(attr, uint, ro)* — Vertical/Horizontal mouse mask
- **MouseOffset** *(attr, uint, ro)* — Vertical/Horizontal mouse offset
- **JournalFlag** *(attr, uint, ro)* — Journaling state/mode
- **JSwapFont** *(attr, uint, ro)* — Jump entry for FMSwapFont
- **WidthListHand** *(attr, uint, ro)* — Handle to list of handles of recent width tables
- **JournalRef** *(attr, uint, ro)* — Refnum of journaling device driver
- **CrsrThresh** *(attr, uint, ro)* — Mouse scaling threshold delta
- **JCrsrTask** *(attr, uint, ro)* — Address of the cursor VBL task
- **WWExist** *(attr, uint, ro)* — Flag indicating Window Manager is init
- **QDExist** *(attr, uint, ro)* — Flag indicating QuickDraw is init
- **JFetch** *(attr, uint, ro)* — Jump vector for driver Fetch routine
- **JStash** *(attr, uint, ro)* — Jump vector for driver Stash routine
- **JIODone** *(attr, uint, ro)* — Jump vector for IODone routine
- **CurApRefNum** *(attr, uint, ro)* — Refnum of current app's res file
- **LoadVars** *(attr, bytes, ro)* — Segment loader variables (region)
- **LaunchFlag** *(attr, uint, ro)* — Flag indicating launch vs. chain
- **FondState** *(attr, uint, ro)* — Saved FOND purge state
- **CurrentA5** *(attr, uint, ro)* — Pointer to current A5 world
- **CurStackBase** *(attr, uint, ro)* — Pointer to current stack base
- **LoadFiller** *(attr, uint, ro)* — Reserved
- **CurApName** *(attr, bytes, ro)* — Name of the current application
- **SaveSegHandle** *(attr, uint, ro)* — Handle to segment 0
- **CurJTOffset** *(attr, uint, ro)* — Current jump table offset from A5
- **CurPageOption** *(attr, uint, ro)* — Sound/screen config for Chain/Launch
- **HiliteMode** *(attr, uint, ro)* — Color highlighting mode flags
- **LoaderPBlock** *(attr, bytes, ro)* — Parameter block for ExitToShell
- **PrintVars** *(attr, bytes, ro)* — Print code variables
- **ChooserBits** *(attr, uint, ro)* — Bit 7=0, don’t run; Bit 6=0, gray out AppleTalk
- **LastPGlobal** *(attr, uint, ro)* — Last Printing Manager global
- **ScrapInfo** *(attr, uint, ro)* — Length of the desk scrap
- **ScrapVars** *(attr, bytes, ro)* — Scrap manager variables (region)
- **ScrapHandle** *(attr, uint, ro)* — Handle to the desk scrap
- **ScrapCount** *(attr, uint, ro)* — Scrap validation count
- **ScrapState** *(attr, uint, ro)* — State of the desk scrap
- **ScrapName** *(attr, uint, ro)* — Pointer to the scrap filename
- **ScrapTag** *(attr, bytes, ro)* — Name of the scrap file
- **ScrapEnd** *(attr, bytes, ro)* — End of scrap variables marker
- **ApFontID** *(attr, uint, ro)* — Application font ID—reset from PRAM
- **GotStrike** *(attr, uint, ro)* — Do we have the strike? (Font Manager)
- **FMDefaultSize** *(attr, uint, ro)* — Default size of Font Record
- **CurFMInput** *(attr, uint, ro)* — Current QuickDraw FMInput Record
- **CurFMSize** *(attr, uint, ro)* — Current font size [short]
- **CurFMFace** *(attr, uint, ro)* — Current font face
- **CurFMNeedBits** *(attr, uint, ro)* — Does Font Manager need bits?
- **CurFMDevice** *(attr, uint, ro)* — Current font device [short]
- **CurFMNumer** *(attr, uint, ro)* — Current numerator of scale factor [point]
- **CurFMDenom** *(attr, uint, ro)* — Current denominator of scale factor [point]
- **FOutRec** *(attr, uint, ro)* — Font Manager output record
- **FOutFontHandle** *(attr, uint, ro)* — Font bits
- **FOutBold** *(attr, uint, ro)* — Bolding factor
- **FOutItalic** *(attr, uint, ro)* — Italic factor
- **FOutULOffset** *(attr, uint, ro)* — Underline offset
- **FOutULShadow** *(attr, uint, ro)* — Underline "halo"
- **FOutULThick** *(attr, uint, ro)* — Underline thickness
- **FOutShadow** *(attr, uint, ro)* — Shadow factor
- **FOutExtra** *(attr, uint, ro)* — Extra horizontal width
- **FOutAscent** *(attr, uint, ro)* — Height above baseline
- **FOutDescent** *(attr, uint, ro)* — Height below baseline
- **FOutWidMax** *(attr, uint, ro)* — Maximum width of character
- **FOutLeading** *(attr, uint, ro)* — Space between lines
- **FOutUnused** *(attr, uint, ro)* — Reserved
- **FOutNumer** *(attr, uint, ro)* — Numerators of scaling factors [point]
- **FOutDenom** *(attr, uint, ro)* — Denominators of scaling factors [point]
- **FMDotsPerInch** *(attr, uint, ro)* — Dots per inch of current device [point]
- **FMStyleTab** *(attr, bytes, ro)* — Style heuristic table supplied by device
- **ToolScratch** *(attr, bytes, ro)* — General purpose 8-byte scratch area
- **WindowList** *(attr, uint, ro)* — Pointer to the window list
- **SaveUpdate** *(attr, uint, ro)* — Enable window update accumulation
- **PaintWhite** *(attr, uint, ro)* — Flag to paint new windows white
- **WMgrPort** *(attr, uint, ro)* — Pointer to the Window Manager's port
- **DeskPort** *(attr, uint, ro)* — Pointer to desk grafPort
- **OldStructure** *(attr, uint, ro)* — Where _DrawNew stores its old value
- **OldContent** *(attr, uint, ro)* — Where _SaveOld stores its old value
- **GrayRgn** *(attr, uint, ro)* — Handle to the desktop region
- **SaveVisRgn** *(attr, uint, ro)* — Temporarily saved visRgn
- **DragHook** *(attr, uint, ro)* — Pointer to general drag hook procedure
- **TempRect** *(attr, bytes, ro)* — Temporary rectangle for Window Manager
- **Scratch8** *(attr, bytes, ro)* — 8-byte scratch area
- **OneOne** *(attr, uint, ro)* — Constant value $00010001
- **MinusOne** *(attr, uint, ro)* — Constant value $FFFFFFFF
- **TopMenuItem** *(attr, uint, ro)* — Used by the Menu Manager to handle scrolling menus
- **AtMenuBottom** *(attr, uint, ro)* — Used by the Menu Manager for scrolling menus
- **IconBitmap** *(attr, uint, ro)* — Used by PlotIcon [bitmap]
- **MenuList** *(attr, uint, ro)* — The current menu list
- **MBarEnable** *(attr, uint, ro)* — If nonzero, menubar belongs to desk accessory
- **CurDeKind** *(attr, uint, ro)* — Window kind of deactivated window
- **MenuFlash** *(attr, uint, ro)* — Number of times to flash menu when selected
- **TheMenu** *(attr, uint, ro)* — Menu ID of the currently highlighted menu
- **SavedHandle** *(attr, uint, ro)* — Saved bits under a menu
- **MrMacHook** *(attr, uint, ro)* — Old name for MBarHook
- **MenuHook** *(attr, uint, ro)* — Procedure called during tracking of the menu
- **DragPattern** *(attr, uint, ro)* — Pattern used to drag controls and windows [pattern]
- **DeskPattern** *(attr, bytes, ro)* — Pattern used to draw the desktop
- **DragFlag** *(attr, uint, ro)* — Implicit parameter to drag control
- **CurDragAction** *(attr, uint, ro)* — Implicit action procedure for DragControl
- **FPState** *(attr, bytes, ro)* — Floating point state
- **TopMapHndl** *(attr, uint, ro)* — Handle to top resource map
- **SysMapHndl** *(attr, uint, ro)* — Handle to system resource map
- **SysMap** *(attr, uint, ro)* — Refnum of system resource file
- **CurMap** *(attr, uint, ro)* — Refnum of current resource file
- **ResReadOnly** *(attr, uint, ro)* — Resource file read-only flag
- **ResLoad** *(attr, uint, ro)* — Current SetResLoad state
- **ResErr** *(attr, uint, ro)* — Last Resource Manager error code
- **TaskLock** *(attr, uint, ro)* — Re-entering system task
- **FScaleDisable** *(attr, uint, ro)* — If true, disables font scaling
- **CurActivate** *(attr, uint, ro)* — Pointer to window getting activate event
- **CurDeactive** *(attr, uint, ro)* — Pointer to window getting deactivate event
- **DeskHook** *(attr, uint, ro)* — Pointer to desktop paint/click hook
- **TEDoText** *(attr, uint, ro)* — Pointer to TextEdit's doText hook
- **TERecal** *(attr, uint, ro)* — Pointer to TextEdit's recalText hook
- **ApplScratch** *(attr, bytes, ro)* — General purpose 12-byte app scratch
- **GhostWindow** *(attr, uint, ro)* — Pointer to a window hidden from FrontWindow
- **CloseOrnHook** *(attr, uint, ro)* — Pointer to routine called when closing desk accessories
- **RestProc** *(attr, uint, ro)* — Resume procedure for InitDialogs
- **ResumeProc** *(attr, uint, ro)* — Pointer to resume procedure
- **SaveProc** *(attr, uint, ro)* — Address of Save failsave procedure
- **SaveSP** *(attr, uint, ro)* — Save SP for restart or save
- **ANumber** *(attr, uint, ro)* — ID if last Alert displayed
- **ACount** *(attr, uint, ro)* — Last Alert stage
- **DABeeper** *(attr, uint, ro)* — Current error sound procedure
- **TEScrapLength** *(attr, uint, ro)* — Length of the TextEdit scrap
- **TEScrpLength** *(attr, uint, ro)* — Length of the TextEdit scrap
- **TEScrpHandle** *(attr, uint, ro)* — Handle to the TextEdit scrap
- **AppPacks** *(attr, bytes, ro)* — Handles to Pack 0 through Pack 7 [8 handles]
- **SysResName** *(attr, bytes, ro)* — Name of the System resource file
- **SoundGlue** *(attr, uint, ro)* — Used by sound glue on Mac XL
- **AppParmHandle** *(attr, uint, ro)* — Handle to Finder information on launch
- **DSErrCode** *(attr, uint, ro)* — Last system error ID
- **ResErrProc** *(attr, uint, ro)* — Pointer to resource error procedure
- **TEWdBreak** *(attr, uint, ro)* — Pointer to default word break routine
- **TEWordBreak** *(attr, uint, ro)* — Pointer to default word break routine
- **DlgFont** *(attr, uint, ro)* — Current font for dialogs
- **TrapAgain** *(attr, uint, ro)* — Used by disk switch hook to repeat File System call
- **KeyMVars** *(attr, uint, ro)* — State for ROM key-mapping procedure
- **ROMMapHndl** *(attr, uint, ro)* — Handle to the ROM resource map
- **PWMBuf1** *(attr, uint, ro)* — PWM buffer pointer
- **BootMask** *(attr, uint, ro)* — Used during boot
- **WidthPtr** *(attr, uint, ro)* — Pointer to global width table (valid only after FMSWapFont)
- **AtalkHk1** *(attr, uint, ro)* — AppleTalk hook
- **AtalkHk2** *(attr, uint, ro)* — AppleTalk hook
- **SCSIFlag** *(attr, uint, ro)* — Configuration flag for SCSI
- **TimeSCSIDB** *(attr, uint, ro)* — SCSI+DBRA instruction loop count
- **WidthTabHandle** *(attr, uint, ro)* — Handle to the global width table
- **SCSIDrvrs** *(attr, uint, ro)* — BitMap for loaded SCSI drivers
- **TimeVars** *(attr, uint, ro)* — Time Manager variables
- **BtDskRfn** *(attr, uint, ro)* — Reference number of boot disk driver
- **BootTmp8** *(attr, bytes, ro)* — Temporary memory used during boot
- **T1Arbitrate** *(attr, uint, ro)* — $FF if VIA timer T1 is available
- **JDiskSel** *(attr, uint, ro)* — Disk driver vector
- **JSendCmd** *(attr, uint, ro)* — Disk driver vector
- **JDCDReset** *(attr, uint, ro)* — Disk driver vector
- **LastSPExtra** *(attr, uint, ro)* — Most recent value of space extra
- **MenuDisable** *(attr, uint, ro)* — Menu ID and item of selected item even if disabled
- **MBDFHndl** *(attr, uint, ro)* — Handle to the current MBDF
- **BNMQHd** *(attr, uint, ro)* — Head of background notification queue
- **RMgrHiVars** *(attr, uint, ro)* — RMGR variations extend $B80 through $B9F
- **RomMapInsert** *(attr, uint, ro)* — Flag to link ROM resource map
- **TmpResLoad** *(attr, uint, ro)* — Temporary ResLoad override value
- **IntlSpec** *(attr, uint, ro)* — Pointer to extra international data
- **RMgrPerm** *(attr, uint, ro)* — Permission byte for OpenResFile
- **WordRedraw** *(attr, uint, ro)* — Flag for TextEdit RecalDraw
- **SysFontFam** *(attr, uint, ro)* — Font ID for the System font
- **SysFontSize** *(attr, uint, ro)* — Font size for the System font
- **MBarHeight** *(attr, uint, ro)* — Height of the menu bar in pixels
- **TESysJust** *(attr, uint, ro)* — TextEdit system justification (L-to-R/R-to-L)
- **HiHeapMark** *(attr, uint, ro)* — High water mark for heap growth
- **SegHiEnable** *(attr, uint, ro)* — Flag to disable MoveHHi in LoadSeg
- **FDevDisable** *(attr, uint, ro)* — $FF to disable device-defined style extra
- **CommToolboxGlobals** *(attr, uint, ro)* — Pointer to Communication Toolbox Globals
- **LastFond** *(attr, uint, ro)* — Last font definition record (FOND)
- **FondID** *(attr, uint, ro)* — ID of last font definition record (FOND)
- **App2Packs** *(attr, bytes, ro)* — Handles to Pack 8 through 15 [8 handles]
- **MAErrProc** *(attr, uint, ro)* — MacApp error procedure
- **MASuperTab** *(attr, uint, ro)* — MacApp superclass table
- **FractEnable** *(attr, uint, ro)* — If true enables fractional font widths
- **UsedFWidths** *(attr, uint, ro)* — Flag saying if fractional widths were used
- **FScaleHFact** *(attr, uint, ro)* — Horizontal font scale factor
- **FScaleVFact** *(attr, uint, ro)* — Vertical font scale factor
- **SCSIBase** *(attr, uint, ro)* — SCSI chip read base address
- **SCSIDMA** *(attr, uint, ro)* — SCSI DMA base address
- **SCSIHsk** *(attr, uint, ro)* — SCSI handshake base address
- **SCSIGlobals** *(attr, uint, ro)* — Pointer to SCSI manager Globals
- **RGBBlack** *(attr, bytes, ro)* — RGB value for black
- **RGBWhite** *(attr, bytes, ro)* — RGB value for white
- **RowBits** *(attr, uint, ro)* — Screen width in pixels
- **ColLines** *(attr, uint, ro)* — Screen height in pixels
- **ScreenBytes** *(attr, uint, ro)* — Total bytes for screen buffer
- **NMIFlag** *(attr, uint, ro)* — NMI debounce flag
- **VidType** *(attr, uint, ro)* — Video board type identifier
- **VidMode** *(attr, uint, ro)* — Video mode (e.g.
- **SCSIPoll** *(attr, uint, ro)* — Flag to poll SCSI device 0 once
- **SEVarBase** *(attr, bytes, ro)* — Base for SE-specific variables
- **MMUFlags** *(attr, uint, ro)* — MMU flags (reserved)
- **MMUType** *(attr, uint, ro)* — Type of Memory Management Unit
- **MMU32bit** *(attr, uint, ro)* — Flag for 32-bit MMU mode
- **MMUFluff** *(attr, uint, ro)* — Unused byte for MMU alignment
- **MMUTbl** *(attr, uint, ro)* — Pointer to MMU mapping table
- **MMUTblSize** *(attr, uint, ro)* — Size of MMU mapping table
- **SInfoPtr** *(attr, uint, ro)* — Pointer to Slot Manager info
- **ASCBase** *(attr, uint, ro)* — Apple Sound Chip base address
- **SMGlobals** *(attr, uint, ro)* — Pointer to Sound Manager Globals
- **TheGDevice** *(attr, uint, ro)* — Pointer to the current GDevice
- **CQDGlobals** *(attr, uint, ro)* — Pointer to Color QuickDraw Globals
- **AuxWinHead** *(attr, uint, ro)* — Auxiliary information for color windows
- **AuxCtlHead** *(attr, uint, ro)* — Auxiliary information for color controls
- **DeskCPat** *(attr, uint, ro)* — PixPatHandle to desk pixpat
- **SetOSDefKey** *(attr, uint, ro)* — Password for SetOSDef
- **ADBBase** *(attr, uint, ro)* — Apple Desktop Bus variables pointer
- **WarmStart** *(attr, uint, ro)* — Flag indicating a warm start
- **TimeDBRA** *(attr, uint, ro)* — DBRA instruction loop count for 1ms
- **TimeSCCDB** *(attr, uint, ro)* — SCC+DBRA instruction loop count
- **SlotQDT** *(attr, uint, ro)* — Pointer to slot queue table
- **SlotPrTbl** *(attr, uint, ro)* — Pointer to slot priority table
- **SlotVBLQ** *(attr, uint, ro)* — Pointer to slot VBL queue table
- **ScrnVBLPtr** *(attr, uint, ro)* — Saved pointer to main screen VBL queue
- **SlotTICKS** *(attr, uint, ro)* — Pointer to slot tick count table
- **AGBHandle** *(attr, uint, ro)* — Handle to AppleTalk global block
- **TableSeed** *(attr, uint, ro)* — Seed for generating color table IDs
- **SRsrcTblPtr** *(attr, uint, ro)* — Pointer to slot resource table
- **JVBLTask** *(attr, uint, ro)* — Jump vector for slot VBL task
- **WMgrCPort** *(attr, uint, ro)* — Window Manager's color GrafPort
- **VertRRate** *(attr, uint, ro)* — Vertical refresh rate for startup
- **SynListHandle** *(attr, uint, ro)* — A handle to a list of synthesized fonts
- **LastFore** *(attr, uint, ro)* — Last foreground color used
- **LastMode** *(attr, uint, ro)* — Last drawing mode
- **LastDepth** *(attr, uint, ro)* — Word used by Font Manager
- **SavedHilite** *(attr, uint, ro)* — Used for state across Becks QD patches
- **ChunkyDepth** *(attr, uint, ro)* — Pixel depth for chunky video modes
- **CrsrPtr** *(attr, uint, ro)* — Pointer to cursor save area
- **PortList** *(attr, uint, ro)* — Linked list of all GrafPorts
- **MickeyBytes** *(attr, uint, ro)* — Pointer to internal cursor data
- **QDErr** *(attr, uint, ro)* — QuickDraw error code
- **VIA2DT** *(attr, bytes, ro)* — VIA2 dispatch table for NuBus Macs
- **SInitFlags** *(attr, uint, ro)* — Flags for the StartInit code
- **DTQueue** *(attr, bytes, ro)* — Deferred task queue header
- **DTQFlags** *(attr, uint, ro)* — Flags word of deferred task queue
- **DTskQHdr** *(attr, uint, ro)* — Pointer to head of deferred task queue
- **DTskQTail** *(attr, uint, ro)* — Pointer to tail of deferred task queue
- **JDTInstall** *(attr, uint, ro)* — Jump vector for deferred task install
- **HiliteRGB** *(attr, bytes, ro)* — RGB value for selection highlighting
- **DSCtrAdj** *(attr, uint, ro)* — Centering adjustment for DS alert box
- **IconTLAddr** *(attr, uint, ro)* — Address for startup icon placement
- **VideoInfoOK** *(attr, uint, ro)* — Flag indicating video card is OK
- **EndSRTPtr** *(attr, uint, ro)* — Pointer to end of Slot Resource Table
- **SDMJmpTblPtr** *(attr, uint, ro)* — Pointer to SDM jump table
- **JSwapMMU** *(attr, uint, ro)* — Jump vector for SwapMMU routine
- **SDMBusErr** *(attr, uint, ro)* — Pointer to Slot Decl Manager bus error handler
- **LastTxGDevice** *(attr, uint, ro)* — Cached GDevice for text measuring

### `lp` *(class lp)*

**Attributes**
- **value** *(attr, uint, ro)*
- **addr** *(attr, uint, ro)*
- **size** *(attr, uint, ro)*
- **pc** *(attr, uint, ro)*
- **instruction_pc** *(attr, uint, ro)*

### `math` *(class math)*

**Methods**
- **`close(`a: float`, `b: float`, `eps: float`)` → `bool`** — True if |a - b| <= eps
- **`abs(`x: float`)` → `float`** — Absolute value
- **`min(`a: float`, `b: float`)` → `float`** — Smaller of two numerics
- **`max(`a: float`, `b: float`)` → `float`** — Larger of two numerics

### `storage` *(class storage)*

**Methods**
- **`import(`host_path: string`, `dst_path?: string`)` → `string`** — Persist a host file under /images/ (deferred — see proposal §5.7)

#### `storage.images` *(class storage_images)*

**Class children**
- **entries** *(child (indexed) → image)*

### `shell` *(class shell)*

#### `shell.alias` *(class alias)*

**Methods**
- **`add(`name: string`, `path: string`)` → `none`** — Register a user alias
- **`remove(`name: string`)` → `none`** — Remove a user alias
- **`list()` → `list`** — List aliases as 'name=path' strings

### `machine` *(class machine)*

**Attributes**
- **model_id** *(attr, string, ro)*
- **model_name** *(attr, string, ro)*
- **cpu_clock_hz** *(attr, uint, ro)*

### `scheduler` *(class scheduler)*

### `memory` *(class memory)*

**Attributes**
- **ram_size** *(attr, uint, ro)*
- **rom_size** *(attr, uint, ro)*

**Methods**
- **`read_cstring(`addr?: uint`, `max_chars?: int`)` → `string`** — Read a quoted, escape-encoded C string at addr

#### `memory.peek` *(class peek)*

**Methods**
- **`b(`addr?: uint`)` → `uint`** — Read 1 byte at addr
- **`w(`addr?: uint`)` → `uint`** — Read 2 bytes (big-endian word) at addr
- **`l(`addr?: uint`)` → `uint`** — Read 4 bytes (big-endian long) at addr

### `cpu` *(class cpu)*

**Attributes**
- **pc** *(attr, uint, ro)*
- **sr** *(attr, uint, ro)*
- **ccr** *(attr, uint, ro)*
- **ssp** *(attr, uint, ro)*
- **usp** *(attr, uint, ro)*
- **msp** *(attr, uint, ro)*
- **vbr** *(attr, uint, ro)*
- **sp** *(attr, uint, ro)*
- **d0** *(attr, uint, ro)*
- **d1** *(attr, uint, ro)*
- **d2** *(attr, uint, ro)*
- **d3** *(attr, uint, ro)*
- **d4** *(attr, uint, ro)*
- **d5** *(attr, uint, ro)*
- **d6** *(attr, uint, ro)*
- **d7** *(attr, uint, ro)*
- **a0** *(attr, uint, ro)*
- **a1** *(attr, uint, ro)*
- **a2** *(attr, uint, ro)*
- **a3** *(attr, uint, ro)*
- **a4** *(attr, uint, ro)*
- **a5** *(attr, uint, ro)*
- **a6** *(attr, uint, ro)*
- **a7** *(attr, uint, ro)*
