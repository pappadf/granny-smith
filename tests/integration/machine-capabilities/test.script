# The real test logic lives in run.sh (it greps the JSON returned by
# machine.profile for each registered model).  The harness requires a
# test.script to exist; this stub is unused by the TEST_RUNNER path.
quit
