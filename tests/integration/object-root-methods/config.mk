# Integration test: top-level root methods.
# Plus is enough; the methods are tree-introspection only.

TEST_NAME := Object-model root methods
TEST_DESC := objects / attributes / methods / help / time on the root

TEST_ROM := roms/Plus_v3.rom
