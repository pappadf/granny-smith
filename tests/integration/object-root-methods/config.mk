# Integration test: M8 slice 3 — top-level root methods
# Plus is enough; the methods are tree-introspection only.

TEST_NAME := Object-model M8 root methods
TEST_DESC := objects / attributes / methods / help / print / time on the root

TEST_ROM := roms/Plus_v3.rom
