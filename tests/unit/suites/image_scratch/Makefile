# The decoded-image scratch cache (src/core/storage/image_scratch.c): names,
# the identity sidecar, and the GS_STORAGE_CACHE root.  09-storage F-33.

TEST_NAME := image_scratch
TEST_SRCS := test.c
TEST_HARNESS := isolated
EXTRA_SRCS := ../../../../src/core/storage/image_scratch.c \
              ../../../../src/core/storage/storage_util.c
include ../../common.mk
