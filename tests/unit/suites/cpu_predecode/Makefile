# 68K predecoded-executor unit test (proposal-predecoded-interpreter-cores.md
# §9.2): coherence (guest stores, host-side writers, the look-back rule),
# cross-page and mid-instruction entries, logpointed pages, pool eviction,
# thrash demotion, and a differential run against the switch core at every
# sprint length and elision level.  CPU_TEST_MODEL=68030 runs it on the 030.
TEST_NAME := cpu_predecode
TEST_SRCS := test.c
TEST_HARNESS := cpu
include ../../common.mk
