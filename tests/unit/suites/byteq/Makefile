# byteq.h -- the bounded, growable byte queue behind PAP's reply queue, its
# capture, and the LaserWriter job's and transport's output.
# Header-only, so there is nothing to link.
TEST_NAME := byteq
TEST_SRCS := test.c
TEST_HARNESS := isolated
include ../../common.mk
