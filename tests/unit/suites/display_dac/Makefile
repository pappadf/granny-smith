# display.h: the direct-colour DAC table (display_t.dac_lut) in the shared
# pixel decode that screen.save / screen.match go through.  See test.c.

TEST_NAME    := display_dac
TEST_SRCS    := test.c
# display.h is header-only; the test supplies main().
TEST_HARNESS := none
STUBS        := assert
include ../../common.mk
